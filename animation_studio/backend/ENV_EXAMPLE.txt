# ==========================================
# ANIMATION STUDIO WAN 2.5 - Configuration
# ==========================================

# ========== APIs REQUISES ==========

# OpenAI (Génération idées et scènes)
OPENAI_API_KEY=sk-proj-votre-cle-openai-ici

# Wavespeed (Wan 2.5 - Génération vidéo avec audio intégré)
WAVESPEED_API_KEY=votre-cle-wavespeed-ici

# ========== CONFIGURATION WAN 2.5 ==========

# Modèle Wan 2.5 (ne pas modifier)
WAN25_MODEL=alibaba/wan-2.5/text-to-video-fast

# Résolution par défaut (720p ou 1080p)
WAN25_DEFAULT_RESOLUTION=720p

# Durée maximale par clip (5 ou 10 secondes)
WAN25_MAX_DURATION=10

# ========== CONFIGURATION SERVEUR ==========

# Port du serveur
PORT=8007

# Host (localhost en dev, 0.0.0.0 en production)
HOST=localhost

# CORS Origins (séparer par des virgules)
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# ========== JWT AUTHENTICATION ==========

# Secret JWT (générer une clé sécurisée en production)
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# ========== CACHE SETTINGS ==========

# Répertoire de cache
CACHE_DIR=../cache

# Taille maximale du cache (Go)
MAX_CACHE_SIZE_GB=10

# Nettoyage du cache (heures)
CACHE_CLEANUP_HOURS=24

# ==========================================
# NOTES IMPORTANTES
# ==========================================

# 1. Audio intégré dans Wan 2.5 - Plus besoin de FAL_API_KEY
# 2. Wan 2.5 génère des clips de 5s ou 10s uniquement
# 3. Résolution 720p recommandée (bon rapport qualité/prix)
# 4. Audio avec lip-sync automatique dans chaque clip
# 5. Prix: $0.34 par clip de 10s en 720p

# Pour Railway: Copier ces variables dans l'onglet "Variables" du service

